class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        s =[]
        for num in nums:
            s.append(num)
        for num in nums:
            s.append(num)
        return s

        